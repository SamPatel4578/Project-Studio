<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

    <style>
        body {
            font-family: Verdana, Geneva, Tahoma, sans-serif;
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-image: url('https://img.freepik.com/free-vector/cartoon-style-gas-station-background_52683-79923.jpg');
            background-size: cover;
            background-position: center;
            position: relative;
            color: white; /* Set text color to white for better visibility */
        }

            /* Apply a dark overlay over the background to enhance contrast */
            body::before {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% transparency */
                z-index: 0; /* Ensure overlay is behind the content */
            }

        /* Ensure content is above the overlay */
        .container {
            position: relative;
            z-index: 1;
        }

        .hero {
            height: 400px;
            width: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7); /* Text shadow to improve visibility */
        }

            .hero h2 {
                font-size: 3rem;
                color: white; /* Changed to white to contrast with the background */
                text-transform: uppercase;
                margin-top: 30px;
                
            }

        .services {
            display: flex;
            justify-content: space-around;
            padding-top: 220px;
            color: white; /* Make sure the text is white */
        }

        .service-item {
            text-align: center;
            width: 30%;
        }

            .service-item h3 {
                font-size: 1.5rem;
                color: white; /* Ensure headings are white */
            }

            .service-item i {
                font-size: 15px;
                color: white; /* Ensure icons are white */
            }
    </style>
</head>
<body>
    <!-- Page-Specific Content -->
    <div class="container">
        <section class="hero">
            <h2>Fueling your journey with <b><i>UTR</i></b></h2>
        </section>

        <section class="services">
            <div class="service-item">
                <i class="bi bi-list-ul"></i>
                <h3>Food Menu</h3>
                <p>Explore different food types and pricing for your stomach.</p>
            </div>
            <div class="service-item">
                <i class="bi bi-fuel-pump"></i>
                <h3>Fuel Options</h3>
                <p>Explore different fuel types and pricing for your vehicle.</p>
            </div>
            <div class="service-item">
                <i class="bi bi-geo-alt"></i>
                <h3>Store Location</h3>
                <p>Grab a snack or drink on the go from our wide selection.</p>
            </div>
            <div class="service-item">
                <i class="bi bi-search"></i>
                <h3>Find Us</h3>
                <p>Locate the UTR station with our store finder tool.</p>
            </div>
        </section>
    </div>

</body>
</html>